import { registry } from '../engine/events/registry.js';

const INTRO = `# Pride Engine

Встроенная документация по редактору и движку.

**Pride Engine** — конструктор 2D-игр прямо в браузере. Рендеринг — WebGPU,
физика — собственный импульсный решатель, события — визуальный event sheet
в стиле Construct.

## Что можно делать

- Рисовать сцену: спрайты, прямоугольники, слои.
- Настраивать физику: статичные, динамические, кинематические тела.
- Собирать логику в **лист событий**: условия → действия.
- Загружать свои PNG-текстуры и звуки.
- Сохранять проект в браузере, в файл \`.pride\`, экспортировать в \`game.html\`.

## С чего начать

1. Откройте вкладку **Сцена**.
2. Разместите объекты и включите им физику в инспекторе справа.
3. Перейдите на вкладку **События** и добавьте первое событие.
4. Нажмите **F5** — игра запустится прямо в редакторе.

> Совет: начните с шаблона «Платформер» — меню **Новый проект**.
`;

const UI = `# Интерфейс

## Верхняя панель

- **?** — справка по горячим клавишам (F1).
- **Книга** — эта документация (F2).
- **Пульс** — профайлер (F3).
- **Солнце / луна** — переключение темы.
- **Терминал** — панель логов (F10).
- **Файл+ / сохранить / папка** — новый проект, снимок, загрузка снимка.
- **Стрелки** — undo / redo.
- **Скачать / загрузить / пакет** — сохранить \`.pride\`, открыть \`.pride\`, экспорт игры.
- **Play / Pause / Stop / Debug** — управление воспроизведением (F5/F6/F7).

## Левая панель

Инструменты: **Выбор** (Q/1), **Прямоугольник** (R/2), **Спрайт** (S/3).

## Вкладки по центру

- **Сцена** — холст и объекты.
- **События** — лист событий: палитра, редактор, переменные.
- **Ресурсы** — текстуры и звуки.
- **Проект** — имя, размер канваса, фон, гравитация.

## Правая панель

**Инспектор** — свойства выбранного объекта.

## Нижняя область

**Слои** — под канвасом, над статус-баром.

## Статус-бар

Координаты мыши, zoom, количество объектов, состояние (редактирование / игра / пауза),
текущий кадр истории.
`;

const SHORTCUTS = `# Горячие клавиши

## Инструменты

- **Q / 1** — выбор
- **R / 2** — прямоугольник
- **S / 3** — спрайт

## Камера

- **СКМ** — панорама
- **Alt + ЛКМ** — панорама
- **Колесо** — масштаб к курсору

## Редактирование

- **Delete / Backspace** — удалить
- **Ctrl+C / X / V** — копировать / вырезать / вставить
- **Ctrl+D** — дублировать
- **Ctrl+A** — выделить всё
- **Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y** — undo / redo
- **Esc** — снять выделение

## Симуляция

- **F5** — играть
- **F6** — пауза
- **F7** — стоп

## Файлы

- **Ctrl+S** — сохранить .pride
- **Ctrl+O** — открыть .pride
- **Ctrl+E** — экспорт игры

## Интерфейс

- **F1** — справка по горячим клавишам
- **F2** — эта документация
- **F3** — профайлер (5 сек записи)
- **F8** — оверлей производительности
- **F9** — бенчмарк (500–5000 объектов)
- **F10** — панель логов
`;

const SCENE = `# Сцена и объекты

## Объекты

Каждый объект имеет:

- **Имя** — используется в событиях как цель (например, \`Player\`).
- **Позиция, размер, поворот, прозрачность**.
- **Слой** — к какому слою привязан.
- **Текстуру** — PNG из вкладки **Ресурсы** или системная \`__white\`.
- **Видимость**.
- **Флаг «Шаблон»** — шаблонные объекты не участвуют в игре и физике,
  но их можно клонировать через действие **Создать объект**.

## Слои

Панель слоёв — под канвасом.

- **Глаз** — видимость слоя.
- **Клик по имени** — выделить все объекты слоя.
- **Двойной клик** — переименовать.
- **Стрелки** — порядок отрисовки.
- **×** — удалить (объекты перейдут на нижний слой).

## Выделение

- **ЛКМ** по объекту.
- **Shift+ЛКМ** — добавить / убрать из выделения.
- **Рамка** по пустому месту — выделение области.
- **Ctrl+A** — всё.
- **Esc** — снять выделение.

## Перемещение

- Тянуть выделенные объекты мышью.
- Менять координаты в инспекторе.
- **Ctrl+C / X / V / D** — копировать, вырезать, вставить, дублировать.
`;

const PHYSICS = `# Физика

## Типы тел

- **static** — не двигается, но сталкивается. Земля, стены.
- **dynamic** — полная симуляция: гравитация, импульсы, столкновения.
- **kinematic** — не двигается сам, но может толкать динамические тела
  (скорость задаётся через действия).

## Форма

- **box** — прямоугольник AABB.
- **circle** — круг радиуса \`radius\`.

## Параметры

- **Плотность** — масса = плотность × площадь × 0.001.
- **Трение** — 0..1, чем выше, тем быстрее гасится скольжение.
- **Упругость** — 0..1, коэффициент отскока.
- **Радиус** — для кругов.

## Отладка

Кнопка **Debug** (перекрестие) в верхней панели — показывает контуры тел
и точки контактов. Полезно, если объект «проваливается» или застревает.

## Гравитация

Настраивается во вкладке **Проект** → **Физика**.
По умолчанию \`(0, 980)\` — вниз. Для вида сверху поставьте \`(0, 0)\`.
`;

const EVENTS_INTRO = `# Лист событий

Лист событий — визуальный «код» игры. Каждое событие — связка
**условий** и **действий**: если все условия выполнены — выполняются действия.

## Структура

- Событие содержит **условия** (сверху) и **действия** (снизу).
- События можно вкладывать: дочернее выполняется, если сработало родительское.
- **Группы** объединяют события и позволяют включать / выключать их пачкой.
- **Комментарии** — цветные заметки, в игре не участвуют.

## Порядок важен

События выполняются сверху вниз. Если два события меняют одну переменную
или одно свойство — сработает верхнее, потом нижнее. Меняйте порядок
перетаскиванием за ручку слева от элемента.

## Как добавить

1. Нажмите **+ Событие**.
2. Нажмите **+ условие** в шапке события, выберите условие.
3. Нажмите **+ действие**, выберите действие.
4. Настройте параметры прямо в строке.

Можно также перетащить условие / действие из палитры слева прямо в событие.

## Отключение

Чекбокс слева от события отключает его. Для группы — аналогично, вся ветка
перестаёт срабатывать.

## Валидация

Если параметр ссылается на несуществующую переменную, объект или звук —
строка подсветится красным, а в шапке события появится значок ⚠ с описанием.
`;

const EVENTS_VARS = `# Переменные

## Глобальные переменные

Панель справа от листа событий.

- **Имя** — идентификатор для событий.
- **Начальное** — значение на старте игры.
- **Текущее** — меняется во время игры.

Действия: **Задать глобальную переменную**, **Прибавить к глобальной переменной**.
Условие: **Сравнить глобальную переменную**.

## Переменные объекта

Задаются в инспекторе в разделе **Переменные объекта**.
У каждого инстанса — своё значение.

Действия: **Задать переменную объекта**, **Прибавить**, **Отнять**.
Условие: **Сравнить переменную объекта**.

## Автопереименование

Если переименовать глобальную переменную в панели или переменную объекта
в инспекторе — все ссылки в событиях обновятся автоматически.
`;

const EVENTS_TIMERS = `# Таймеры

В Pride Engine два вида таймеров: **локальные** (привязаны к конкретному
событию) и **именованные** (глобальные, живут вне событий).

## Локальные таймеры

Три условия в категории **Таймеры**:

- **Каждые N секунд** — срабатывает повторно каждые N секунд работы события.
- **Через N секунд** — срабатывает один раз, когда с момента старта игры
  прошло N секунд.
- **Через N кадров** — как предыдущее, но отсчитывает кадры (тики физики).

Локальные таймеры не требуют создавать переменные и вручную считать время
в **Каждый кадр**.

## Именованные таймеры

Для случаев, когда таймер привязан к состоянию объекта (перезарядка
оружия, неуязвимость, кулдаун урона), есть именованные таймеры.
Имя — обычная строка, например \`reload\` или \`invuln\`.

**Действия:** Запустить, Остановить, Продолжить, Сбросить.

**Условие:** **Таймер: прошло N секунд**.

## Паузы и перезапуск

Таймеры сбрасываются при Play / Stop и не тикают во время паузы:
время идёт только в фазе игры.
`;

const EVENTS_SOUND = `# Звук в событиях

## Загрузка

Звуки загружаются во вкладке **Ресурсы** → **Загрузить звук**. Поддерживаются
MP3, OGG, WAV, WebM. Файлы сохраняются в браузере (IndexedDB) и декодируются
в фоне — редактор остаётся отзывчивым даже при загрузке десятка треков.

## Проигрывание

Действие **Проиграть звук** запускает звук. Параметры:

- **Звук** — имя файла из вкладки «Ресурсы».
- **Громкость** — 0..1. Для фоновой музыки ставьте 0.3–0.5,
  чтобы не заглушать звуки событий.
- **Цикл** — если true, звук повторяется бесконечно, пока его не остановят.
- **Панорама** — -1 (левый канал) до 1 (правый). 0 — по центру.
- **Тон** — 1 — обычная высота. 0.5 — на октаву ниже, 2 — на октаву выше.
  Полезно для случайного питча у выстрелов: 0.9–1.1 даёт лёгкое разнообразие.

Каждый вызов **Проиграть звук** создаёт независимое проигрывание —
один и тот же звук может играть несколько раз одновременно. Для выстрелов
и ударов это нормально; для фоновой музыки используйте «Цикл = true».

## Остановка

- **Остановить звук** — останавливает все активные проигрывания с этим именем.
- **Остановить все звуки** — глушит всё. Полезно при переходе на новый экран
  или в меню.

## Управление во время игры

- **Задать общую громкость** — master gain, действует на всё.
  Хорошо подходит для слайдера в настройках.
- **Задать панораму звука** — для активных проигрываний с этим именем.
  Позволяет имитировать позиционный звук без движка 3D-аудио.
- **Задать тон звука** — меняет скорость воспроизведения (и высоту тона).
- **Плавно увеличить громкость** — постепенно поднимает громкость **уже
  играющих** проигрываний указанного звука до заданного уровня за N секунд.
  Само по себе звук не запускает: работает только с тем, что уже играет.
  Обычно используется для фоновой музыки: сначала **Проиграть звук**
  с громкостью 0, затем **Плавно увеличить громкость** до нужного уровня.
- **Плавно уменьшить громкость** — постепенно гасит громкость **уже
  играющих** проигрываний до нуля за N секунд, после чего останавливает их.
  Звук не запускает.

## Условия

- **Звук играет** — истинно, пока есть активное проигрывание этого звука.
- **Звук завершился** — срабатывает один раз, когда звук доиграл до конца
  естественно. **Ручная остановка, плавное затухание и stopAll завершением
  не считаются** — для них есть отдельные вызовы, и условие не сработает.
  Это удобно, чтобы не запускать «следующий трек» после принудительной
  остановки.

## Пример: фоновая музыка с затуханием при выходе

**Событие 1** (при старте игры):
- Условие: **Через N секунд** = 0.
- Действие 1: **Проиграть звук** \`music_theme\`, громкость 0, цикл true.
- Действие 2: **Плавно увеличить громкость** \`music_theme\`, 2 секунды,
  громкость 0.5.

Порядок действий важен: **сначала запускаем звук, потом поднимаем громкость**.
Если поменять местами — поднимать будет нечего, потому что проигрывания
ещё нет.

**Событие 2** (при переходе в меню, скажем по кнопке Esc):
- Действие: **Плавно уменьшить громкость** \`music_theme\`, 1 секунда.

## Пример: звук выстрела с вариацией тона

Событие: **При нажатии клавиши** Space.
- Действие: **Проиграть звук** \`shot\`, громкость 0.8,
  панорама 0, тон 0.95.

Тон 0.95 — на 5% ниже обычного. Можно рандомизировать вручную,
создавая несколько событий, или использовать переменную для динамики.

## Экспорт

Звуки, использованные в событиях, упаковываются в экспортированную игру
автоматически. Неиспользуемые в событиях — не попадают, как и текстуры.
В single-режиме — как data URL внутри HTML, в folder-режиме — отдельные
файлы в папке \`audio/\`.
`;

const EVENTS_REF_INTRO = `# Справочник условий и действий

Ниже — автогенерируемые разделы из реестра. Они собираются прямо при
открытии документации: если вы зарегистрируете новое условие или действие —
оно сразу появится в списке слева.

- **Справочник условий** — все доступные условия, сгруппированы по категориям.
- **Справочник действий** — аналогично для действий.

## Общие типы параметров

- **target** — имя объекта на сцене или \`*\`.
- **varname** — имя глобальной переменной.
- **instvar** — имя переменной объекта.
- **prefab** — имя объекта-шаблона (для спавна).
- **sound** — имя звука из вкладки «Ресурсы».
- **number**, **string**, **select**, **key** — обычные значения.
`;

const ASSETS = `# Ресурсы

## Текстуры

Вкладка **Ресурсы** → **Загрузить PNG**. Поддерживаются PNG, JPEG, WebP.

## Атлас

Все загруженные PNG упаковываются в один большой атлас 4096×4096.
Это позволяет рисовать всю сцену за один draw call. Если загружено
очень много картинок и они не влезают — создаётся вторая страница
атласа, и появляется второй draw call.

## Системная текстура

\`__white\` — белый пиксель 1×1. Не отображается в списке. Используется
для объектов без своей текстуры (прямоугольники, отладочные линии).

## Звуки

**Загрузить звук** — принимает MP3, OGG, WAV, WebM. Файл сохраняется
в IndexedDB и декодируется в фоне — редактор не блокируется даже при
загрузке десятка треков.

- **▶** в строке звука — прослушать прямо в редакторе.
- Пока звук декодируется, под именем видна полоса загрузки.
- В заголовке секции показывается счётчик: «3 / 5» — три из пяти готовы.

## Изоляция по проекту

Ассеты каждого проекта хранятся отдельно. При открытии нового проекта
список ресурсов подменяется автоматически — старые текстуры и звуки не мешают.

## Переименование и удаление

Клик по имени — переименование; ссылки в сцене и событиях обновятся
автоматически. Удаление — кнопка ×. Пиксели удалённой текстуры остаются
в атласе до полного пересоздания (новый проект / открытие файла).
`;

const PROJECT = `# Проект и экспорт

## Настройки

Вкладка **Проект**:

- **Имя** — метаданные, отображается в заголовке.
- **Ширина / высота** канваса.
- **Цвет фона**.
- **Гравитация X / Y**.
- **Показывать подсказки** — глобальная настройка, хранится в браузере.

## Автосохранение

Проект автоматически сохраняется в localStorage браузера. Индикатор
в статус-баре показывает состояние: \`unsaved\` → \`saved\`.

## Снимки

**Сохранить** в верхней панели — именованная точка сохранения.
**Загрузить** — список снимков, восстановление или удаление.

## Файл .pride

- **Ctrl+S** — скачать \`.pride\` (gzip + JSON).
- **Ctrl+O** — открыть \`.pride\`.

## Экспорт в HTML

**Ctrl+E** — открывается окно с настройками. Формат:

- **Один файл (game.html)** — всё встроено: модули, ассеты, favicon.
- **Папка (game.zip)** — отдельные модули, \`assets/\`, \`audio/\`, \`favicon.svg\`.
  Требуется локальный сервер или публикация.

## Опции экспорта

- **Сжать модули** — удаляет комментарии и пустые строки из JS.
- **Splash-экран при запуске** — анимированный логотип, ~0.8 секунды.
- **Показывать FPS в игре** — счётчик кадров в углу.
- **Показывать контуры физики** — debug draw, обычно выключено.

## Что попадает в экспорт

- **Текстуры** — только те, что назначены объектам сцены.
- **Звуки** — только те, что используются в действиях и условиях событий.

Неиспользуемые ресурсы не упаковываются — это уменьшает размер игры.
`;

export const DOCS = [
  { id: 'intro',        title: 'Введение',            category: 'Начало',     content: INTRO },
  { id: 'ui',           title: 'Интерфейс',           category: 'Начало',     content: UI },
  { id: 'shortcuts',    title: 'Горячие клавиши',     category: 'Начало',     content: SHORTCUTS },

  { id: 'scene',        title: 'Сцена и объекты',     category: 'Сцена',      content: SCENE },
  { id: 'physics',      title: 'Физика',              category: 'Сцена',      content: PHYSICS },

  { id: 'events-intro', title: 'Как работает лист',   category: 'События',    content: EVENTS_INTRO },
  { id: 'events-vars',  title: 'Переменные',          category: 'События',    content: EVENTS_VARS },
  { id: 'events-timers',title: 'Таймеры',             category: 'События',    content: EVENTS_TIMERS },
  { id: 'events-sound', title: 'Звук',                category: 'События',    content: EVENTS_SOUND },
  { id: 'events-ref',   title: 'Справочник: обзор',   category: 'События',    content: EVENTS_REF_INTRO },

  { id: 'assets',       title: 'Ресурсы',             category: 'Ресурсы',    content: ASSETS },
  { id: 'project',      title: 'Проект и экспорт',    category: 'Проект',     content: PROJECT },
];

export function buildRefDocs() {
  return [
    {
      id: 'ref-conditions',
      title: 'Справочник условий',
      category: 'Справочник',
      content: buildRef('conditions'),
    },
    {
      id: 'ref-actions',
      title: 'Справочник действий',
      category: 'Справочник',
      content: buildRef('actions'),
    },
  ];
}

function buildRef(kind) {
  const map = kind === 'conditions' ? registry.conditions : registry.actions;
  const all = map.all();

  const byCat = new Map();
  for (const it of all) {
    const cat = it.category || 'Прочее';
    if (!byCat.has(cat)) byCat.set(cat, []);
    byCat.get(cat).push(it);
  }

  const kindRu = kind === 'conditions' ? 'Условия' : 'Действия';
  let md = `# Справочник: ${kindRu}\n\n`;
  md += `Всего элементов: **${all.length}**. Сгруппированы по категориям.\n\n`;

  for (const [cat, items] of byCat) {
    md += `## ${cat}\n\n`;
    for (const it of items) {
      md += `### ${it.label || it.id}\n\n`;

      if (it.description) {
        md += `${it.description}\n\n`;
      }

      md += `Идентификатор: \`${it.id}\`\n\n`;

      if (it.params && it.params.length) {
        md += 'Параметры:\n\n';
        for (const p of it.params) {
          md += `- **${p.label || p.id}**: тип \`${p.type}\`, id \`${p.id}\``;
          if (p.default !== undefined) {
            md += `, по умолчанию \`${JSON.stringify(p.default)}\``;
          }
          if (p.options && p.options.length) {
            md += `, варианты: ${p.options.map((o) => '`' + o + '`').join(', ')}`;
          }
          md += '\n';
        }
        md += '\n';
      } else {
        md += 'Без параметров.\n\n';
      }
    }
  }

  return md;
}